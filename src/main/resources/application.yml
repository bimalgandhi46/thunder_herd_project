spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    # password injected via Ansible template at deploy time
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

server:
  port: 8080

spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS: false
